export function LoginPage() {
  return (
    <section className="page-card">
      <p className="page-card__eyebrow">Authentication</p>
      <h2>Login page scaffold</h2>
      <p className="page-card__body">
        The backend already exposes a token endpoint, so this route is ready for the real
        sign-in form when we wire the API layer.
      </p>
    </section>
  );
}
