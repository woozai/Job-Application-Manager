# Backend

FastAPI backend for the Job Application Manager.

## Setup

```powershell
uv sync
```

## Run

```powershell
uv run uvicorn app.main:app --reload
```

The API starts at `http://127.0.0.1:8000`.

## Test

```powershell
uv run pytest
```
